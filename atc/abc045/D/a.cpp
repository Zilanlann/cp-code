#include <iostream>
#include <vector>
using namespace std;
typedef long long ll;

vector<int> ve;

int main() {
    ve.push_back(1);
    ve.push_back(2);
    cout << ve.front() << " " << ve.back() << "\n";

    return 0;
}
